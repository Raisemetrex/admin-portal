
import React from 'react';

export const MenuContext = React.createContext({
  // posts: [],
  // users: [],
  // addPost: (props) => {
  //   console.log('addPost:', { props, self: this );
  //   // posts.push(props);
  // },
  // addUser: (props) => { console.log('addUsers:', props); },
});

export default MenuContext;
